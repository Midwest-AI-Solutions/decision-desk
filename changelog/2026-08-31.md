# decision-desk changelog

## 2026-08-31

- Repo scaffolded (post-registration, inside the Aug 10–Sep 14 submission
  period): package skeleton `src/decision_desk/` (models, provider, pipeline,
  agent, inbox, CLI), synthetic fixtures for 5 scenarios, 15 pytest cases,
  MIT LICENSE, README, ARCHITECTURE.md with SVG architecture diagram.
- Triaged pipeline runs deterministic-first (safety rules never depend on a
  model call); `echo` provider default keeps the project judge-runnable with
  zero cloud setup, `DECISION_DESK_PROVIDER=bedrock` is the powered path.
- Verified: `pytest` 15/15 green (Python 3.9, `PYTHONPATH=src`); compile-check
  clean on 3.13; CLI end-to-end over fixtures → 2 emergencies / 1 decision /
  2 silent; inbox.html rendered; JSON mode valid.
- Decision inbox upgraded from static mock to interactive decision surface:
  approve/tweak/decline persist per card in localStorage (date-scoped keys),
  saved-action counter restores on reload, reset-demo and show-silent controls,
  escalation details rendered per card (quote range, proposed slot, prep,
  dispatch note, callback/reply script). Defects fixed en route: duplicate
  BUSINESS_NAME definitions removed; stale XSS assertion corrected to assert
  user payload is escaped (our own script bootstrap is intentionally present).
- Verified live in a browser engine, not just by unit tests: 18/18 pytest
  green (Python 3.9), compileall clean on 3.13; DOM/driver run confirmed
  approve -> reload -> state restored ('1 action decided in earlier
  sessions'), decline, reset clears card + storage, silent cards display:none
  default and block after toggle. CLI end-to-end unchanged: 2 emergencies /
  1 decision / 2 silent.
- Agent tool-wrapper contract tests added (tests/test_agent_tools.py): the
  three Strands tools' JSON schema is now pinned (keys, urgency ordering,
  quote range invariants, error paths). Judge-facing surface is regression-
  protected without requiring strands to be installed (echo mode).
- Fixture set now demonstrates all four triage outcomes (emergency x2,
  quote decision, default-up unknown, routine x2) for the demo flow.
- Run-124 harden block (Sep 8–9 plan): docs/diagram aligned with the real,
  test-pinned 3-tool Strands surface (`triage_message`, `draft_next_action`,
  `run_intake_cycle`) — the SVG and ARCHITECTURE.md had drifted to four
  invented tool names; "LLM resolves ambiguous classifications" corrected to
  the actual deterministic-first design (LLM only rephrases routine answers,
  powered mode only). Powered mode made real: the CLI now wires
  `load_provider()` (previously dead code) into routine-answer phrasing;
  echo-mode output byte-identical (verified). README gained a submission
  deliverables map + Deployment section. AgentCore Runtime deployment
  package PREPARED, owner-run (`deployment/`: BedrockAgentCoreApp
  entrypoint, requirements, runbook with official-docs commands) + 3
  contract tests for the entrypoint wrapper (27 total green). Final
  deliverables for owner approval: demo video script v2 +
  Devpost entry text v2 (`workspace/`).
